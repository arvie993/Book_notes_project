<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book.title %> - Book Details</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>📖 Book Details</h1>
      <a href="/" class="btn-back">← Back to Library</a>
    </header>

    <div class="book-detail">
      <div class="book-detail-cover">
        <% if (book.cover_url && book.isbn) { %>
          <img src="<%= book.cover_url %>" alt="<%= book.title %> cover" onerror="this.src='/css/placeholder-book.png'; this.onerror=null;">
        <% } else { %>
          <div class="cover-placeholder-large">
            <span>📖</span>
          </div>
        <% } %>
      </div>

      <div class="book-detail-info">
        <h2><%= book.title %></h2>
        <p class="author">by <%= book.author %></p>

        <% if (book.isbn) { %>
          <p class="isbn"><strong>ISBN:</strong> <%= book.isbn %></p>
        <% } %>

        <% if (book.rating) { %>
          <div class="rating-large">
            <div class="stars">
              <% for (let i = 0; i < 10; i++) { %>
                <span class="<%= i < book.rating ? 'filled' : '' %>">★</span>
              <% } %>
            </div>
            <span class="rating-number"><%= book.rating %>/10</span>
          </div>
        <% } %>

        <% if (book.date_read) { %>
          <p class="date-read-large">
            <strong>Read on:</strong> <%= new Date(book.date_read).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
          </p>
        <% } %>

        <% if (book.notes) { %>
          <div class="notes-full">
            <h3>My Notes</h3>
            <p><%= book.notes %></p>
          </div>
        <% } %>

        <div class="book-actions-detail">
          <a href="/edit/<%= book.id %>" class="btn btn-primary">Edit</a>
          <form action="/delete/<%= book.id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this book?');">
            <button type="submit" class="btn btn-delete">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
